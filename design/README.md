# Design sources

Not used by the build. These live here so the originals are versioned
somewhere rather than sitting loose in the repo root.

- `logo.af` — Affinity source for the mark
- `logo.png` — 144×144 export; the favicons in `public/` derive from this
- `Recolour Adjustment.png` — 10×16px, almost certainly a stray Affinity
  export. Safe to delete if you don't recognise it.
